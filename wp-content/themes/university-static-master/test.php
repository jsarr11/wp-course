<?php

function doubleMe($k) {
    $l =  $k * 2;
    die(var_dump($l)); exit();
    return $l;
}
doubleMe(20);

//the ones with get just return something, so they need echo
//the ones not, they echo something
the_title();
get_the_title();

the_ID();
get_the_id();

?>